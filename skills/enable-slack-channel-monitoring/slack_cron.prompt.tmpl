[scheduled: {{MONITOR_NAME}}] FIRST, run Bash `/home/claude/workspaces/bin/slack-lock acquire {{MONITOR_NAME}}`. If it prints `LOCKED` → a previous monitor run is still active (its 3-minute polling extended past this fire); SKIP this fire entirely: spawn nothing, emit nothing, end the turn. If it prints `ACQUIRED` → proceed.

Spawn EXACTLY ONE subagent (Agent tool, general-purpose) to monitor Slack channel {{CHANNEL_NAME}} (channel_id {{CHANNEL_ID}}) following the rules in {{PROJECT_DIR}}/SLACK_CRON.md. The subagent does ALL message-reading, reasoning, and replying in its OWN context via the Slack MCP tools (load with ToolSearch: mcp__claude_ai_Slack__slack_read_channel, mcp__claude_ai_Slack__slack_send_message). Key rules: only consider NEW messages ≤8h old; if none new/eligible → stay completely silent; reply ONLY when clearly warranted (a genuinely quick+safe experiment runnable in {{PROJECT_DIR}} via `uv run`, a literature "does X exist" question, an implementation-detail question answerable from the project code, or a clear factual error) — accuracy over engagement, never post speculation, stay collegial; every post begins with an automated-agent disclaimer line; post via slack_send_message to {{CHANNEL_ID}} (use thread_ts to reply in-thread). After each reply, poll for responses in 3 × 3-minute intervals per SLACK_CRON.md (run `/home/claude/workspaces/bin/slack-lock refresh {{MONITOR_NAME}}` after each poll so the lock stays fresh during a live run); then end. Durable hand-off: append only noteworthy dated bullets to {{PROJECT_DIR}}/research/slack_monitor_notes.md.

AFTER the subagent returns, ALWAYS run Bash `/home/claude/workspaces/bin/slack-lock release {{MONITOR_NAME}}` (even if the subagent errored). PARENT SESSION (you): aside from the slack-lock acquire/release Bash calls, emit NOTHING — no narration, no summary; discard the subagent's terse status silently.
