[scheduled: {{MONITOR_NAME}}] FIRST run Bash `/home/claude/workspaces/bin/slack-lock acquire {{MONITOR_NAME}}`. `LOCKED` → prior run still active (its 3-min polling overran this fire); SKIP entirely: spawn nothing, emit nothing, end turn. `ACQUIRED` → proceed.

Spawn EXACTLY ONE subagent (Agent tool, general-purpose) to monitor Slack channel {{CHANNEL_NAME}} (channel_id {{CHANNEL_ID}}) per {{PROJECT_DIR}}/SLACK_CRON.md. Subagent does ALL message-reading/reasoning/replying in its OWN context via Slack MCP (ToolSearch: mcp__claude_ai_Slack__slack_read_channel, mcp__claude_ai_Slack__slack_send_message). Key rules: only NEW messages ≤8h old; none new/eligible → stay silent; reply ONLY when clearly warranted (quick+safe `uv run` experiment in {{PROJECT_DIR}}, literature "does X exist" question, impl-detail question answerable from project code, or clear factual error) — accuracy over engagement, no speculation, collegial; every post starts with an automated-agent disclaimer line; post via slack_send_message to {{CHANNEL_ID}} (thread_ts to reply in-thread). After each reply, poll 3 × 3-min per SLACK_CRON.md (`/home/claude/workspaces/bin/slack-lock refresh {{MONITOR_NAME}}` after each poll to keep lock fresh); then end. Hand-off (per SLACK_CRON.md "Hand-off to the project worker"): code work → enqueue task block in {{TASKS_FILE}}, nudge worker via `/home/claude/workspaces/bin/tell-worker {{WORKER_NAME}} "…"`, ack in-thread; at START of each wake post back any worker `-> reply:` results to their threads. Durable memory: append dated bullets to {{NOTES_FILE}} (monitor log, not worker queue).

AFTER the subagent returns, ALWAYS run Bash `/home/claude/workspaces/bin/slack-lock release {{MONITOR_NAME}}` (even if it errored). PARENT (you): aside from slack-lock acquire/release, emit NOTHING — no narration/summary; discard the subagent's status. (Subagent nudges the worker itself; worker's `-> reply:` posts on the next tick, not by you.)
